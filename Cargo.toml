[package]
name = "treex"
version = "0.1.0"
edition = "2021"
description = "A tree.exe-like directory tree viewer"
license = "MIT"

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = 3
panic = "abort"
